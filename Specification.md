# Image Resizer Specification

## 1. 概要

Image Resizerは、画像の長辺を指定したピクセル数に合わせてリサイズするデスクトップ・Webアプリケーションである。

複数の画像を一度に処理でき、元画像を変更せず、リサイズ後の画像を別フォルダへ保存する。

## 2. 目的

画像をWeb掲載、SNS投稿、メール送信、資料作成などに使用する際に発生する、

* 画像サイズが大きすぎる
* 画像を1枚ずつリサイズするのが面倒
* 複数画像を同じサイズに揃えたい

といった作業を簡単にする。

## 3. 基本方針

本アプリケーションは単機能ツールとして設計する。

### 基本原則

1. 操作をできるだけ少なくする
2. 元画像を変更しない
3. 複数画像を一括処理する
4. 縦横比を維持する
5. オフラインで処理する
6. 不要な機能を追加しない

## 4. 対応画像形式

### 入力

* JPG
* JPEG
* PNG
* WebP

### 出力

原則として入力形式を維持する。

## 5. リサイズ仕様

ユーザーは画像の「長辺」をピクセル単位で指定する。

### 横長画像

```text
元画像
4000 × 3000

指定
1280px

結果
1280 × 960
```

### 縦長画像

```text
元画像
3000 × 4000

指定
1280px

結果
960 × 1280
```

### 正方形

```text
元画像
4000 × 4000

指定
1280px

結果
1280 × 1280
```

## 6. 小さい画像の扱い

指定サイズよりも元画像の長辺が小さい場合、原則として拡大しない。

例：

```text
元画像
800 × 600

指定
1280px

結果
800 × 600
```

これにより、不要な画質劣化を防ぐ。

## 7. 入力方法

以下の方法を基本とする。

### ドラッグ＆ドロップ

画像ファイルをアプリケーションへドラッグ＆ドロップする。

複数ファイルを同時にドロップ可能とする。

## 8. UI仕様

画面は可能な限りシンプルにする。

```text
┌──────────────────────────────┐
│        Image Resizer         │
│                              │
│  ここに画像をドロップ         │
│                              │
│  長辺： [ 1280 ] px           │
│                              │
│  出力先： [ resized ]         │
│                              │
│          [ 変換開始 ]         │
└──────────────────────────────┘
```

## 9. 出力先

デフォルトでは、入力画像が存在するフォルダ内に `resized` フォルダを作成する。

```text
input/
├── image01.jpg
├── image02.jpg
└── resized/
    ├── image01.jpg
    └── image02.jpg
```

## 10. 元画像の保護

元画像を直接上書きしてはならない。

リサイズ処理では必ず別ファイルとして保存する。

## 11. 同名ファイル

出力先に同名ファイルが存在する場合、既存ファイルを無条件で上書きしない。

基本動作は連番などによる別名保存とする。

例：

```text
image.jpg
image_001.jpg
image_002.jpg
```

## 12. エラー処理

以下の場合はエラーとして扱う。

* 対応していない画像形式
* 画像ファイルが破損している
* サイズ指定が不正
* 出力先へ書き込めない
* 画像の読み込みに失敗した

エラー発生時には、処理を停止するのではなく、可能な範囲で他の画像の処理を継続する。

## 13. 処理結果

処理終了後、以下の情報を表示する。

```text
処理完了

成功：8
失敗：1

出力フォルダを開く
```

必要に応じて失敗したファイルと理由を表示する。

## 14. 非機能要件

### オフライン動作

画像処理のために外部サーバーへ画像を送信しない。

### パフォーマンス

複数画像をまとめて処理できること。

一般的なデジタルカメラ・スマートフォン画像を実用的な速度で処理できること。

### 安全性

元画像を変更しないことを基本とする。

## 15. 対象ユーザー

特定の専門職を対象とせず、一般的なPCユーザーを対象とする。

想定用途：

* SNS投稿
* Webサイト掲載
* メール添付
* ブログ
* 資料作成
* 写真整理
* フリマ・オークションへの画像掲載

## 16. スコープ外

初期バージョンでは以下を実装対象外とする。

* 画像編集
* トリミング
* 回転
* フィルター
* 明るさ調整
* AI画像処理
* 顔認識
* クラウドアップロード
* 画像管理機能

## 17. MVP

最初のリリースでは以下だけを実装する。

* 画像のドラッグ＆ドロップ
* 複数画像対応
* 長辺指定
* 縦横比維持
* 小さい画像を拡大しない
* 元画像を保持
* `resized` フォルダへの保存
* JPG / JPEG / PNG / WebP対応
* 処理結果表示

これらが動作すれば、初期版として公開可能とする。

## 18. ライセンス

MIT Licenseを採用する。
