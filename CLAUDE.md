# Claude Code Rules (CLAUDE.md)

このプロジェクトの開発ルール正本は **`AI_RULES.md`** に一元管理されています。
作業開始前に必ず `AI_RULES.md` を読み込み、記載されたすべての指示に従ってください。

---

## 主要ルール抜粋（AI_RULES.md より）

### 【最最厳守】事前承認の義務
1. 作業開始前に必ずチャット上で「これから何を行うか」を提示し、合意を形成すること。
2. ユーザーから明確な承認を得るまでコード変更・コマンド実行を含む実作業を一切開始しないこと。
3. 承認後は承認された範囲のみを正確に実行すること。

### コミュニケーション
- 日本語で回答すること。
- 過剰な修飾語・大げさな表現を排除し、客観的・簡潔・技術的な事実のみを伝えること。

### 品質監査
- 実装・修正完了時は `run_project_quality_audit`（MCP）または `v3.run_quality_audit`（V3MCP）を呼び出すこと。
- MCPが使えない場合は quality-audit スキルを手動適用すること。
- テスト通過・ビルド成功だけを根拠にした「PASS」判定は禁止。

### MCPツール優先
- ファイル調査 → `build_context_pack`
- 実装開始前 → `v3.run_pre_audit`
- 実装完了後 → `run_project_quality_audit` または `v3.run_quality_audit`
- セッション終了前 → `generate_handover`

### バージョン管理
- 作業完了後は必ず `git commit` を実行すること（マイクロコミット）。

### その他
- 不要なリファクタリング禁止（指示と無関係のコードに触れない）。
- `.bat` ファイルは Shift-JIS (CP932) で保存すること。
- ファイルが 300 行を超えたらユーザーへ分割を提案すること。