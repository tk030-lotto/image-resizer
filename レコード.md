# 開発記録(レコード)— Image Resizer

- **日付**: 2026/08/25
- **プロジェクト**: 画像リサイズ(c:\Users\tk030\Desktop\画像リサイズ)
- **成果物**: 長辺指定で画像を一括リサイズするWebツール(TypeScript / 完全クライアントサイド)

---

## 1. 開発の流れ

| フェーズ | 内容 | 結果 |
|---|---|---|
| 1. 計画 | README.md / Specification.md を読み込み要件整理。実装計画を作成しユーザー承認 | ✅ |
| 2. 技術選定 | ユーザーの指示により「TypeScript/JavaScript の Web ツール」と確定 | ✅ |
| 3. 環境確認 | Node v22.22.3 / npm 10.9.8 を確認(Vite 7 の要件を満たす) | ✅ |
| 4. 実装 | 設定ファイル・HTML/CSS・コアロジック・I/O・UI・テスト計17ファイルを作成 | ✅ |
| 5. テスト | Vitest でユニットテスト実行 → 1件失敗を検出 → 修正 → 再実行 | ✅ 33/33 |
| 6. ビルド | tsc --noEmit(型チェック)+ vite build(単一HTML生成) | ✅ 成功 |
| 7. ドキュメント | README.md を実装内容に合わせ更新 | ✅ |

## 2. 要件サマリー(Specification.md より)

- デスクトップアプリ仕様書だったが、ユーザー判断により**ブラウザで動く Web ツール**として実装
- 「オフライン動作」「外部サーバーへの画像アップロードなし」は、処理を完全にクライアントサイド(Canvas API)で行うことで満たす設計とした
- MVP スコープ: D&D入力 / 複数画像 / 長辺指定 / 縦横比維持 / 拡大禁止 / 元画像保持 / resizedフォルダ保存 / JPG・JPEG・PNG・WebP / 処理結果表示

## 3. 技術選定と理由

| 項目 | 選定 | 理由 |
|---|---|---|
| 言語 | TypeScript | サイズ計算・ファイル名処理など純粋ロジックの型安全性・テスト容易性 |
| ビルド | Vite 7 + vite-plugin-singlefile | 単一 dist/index.html にインライン化でき、配布・オフライン利用が容易 |
| フレームワーク | なし(Vanilla TS) | 画面は仕様§8の1画面のみ。React等は過剰 |
| 画像処理 | ブラウザ標準API(createImageBitmap / Canvas / toBlob) | ライブラリ不要・完全クライアントサイド |
| ZIP生成 | fflate(level 0=無圧縮格納) | FSAA非対応ブラウザ向けフォールバック。画像は既に圧縮済みのため level 0 |
| テスト | Vitest | コアロジック(純粋関数)のユニットテスト |

## 4. ファイル構成(最終)

```text
画像リサイズ/
├── index.html              # UI本体(仕様§8のワイヤーフレーム準拠)
├── package.json            # scripts: dev/build/test/preview
├── tsconfig.json           # strict / noEmit / bundler 解決
├── vite.config.ts          # vite-plugin-singlefile で単一HTML化
├── .gitignore              # node_modules / dist
├── dist/index.html         # ビルド成果物(24.2 kB / gzip 10.7 kB)
├── src/
│   ├── main.ts             # UI結線(D&D・変換フロー・進捗・結果表示)
│   ├── style.css           # スタイル(ライト/ダーク両対応)
│   ├── core/
│   │   ├── resize.ts       # calculateDimensions + リサイズパイプライン(EXIF回転補正)
│   │   ├── filename.ts     # resolveFileName(同名時 image_001.jpg 形式連番)
│   │   └── format.ts       # detectFormat / resolveEncodeMime / supportsWebpEncode
│   ├── io/
│   │   ├── drop.ts         # DataTransfer からの File 抽出
│   │   └── writer.ts       # FSAA書き込み / sanitizeFolderName / ZIP生成・ダウンロード
│   └── ui/
│       └── result.ts       # 処理結果レンダリング(textContentのみでXSS安全)
└── tests/
    ├── resize.test.ts      # 寸法計算(横長/縦長/正方形/拡大禁止/丸め/境界値)
    ├── filename.test.ts    # 分割・連番・大小文字同一扱い
    ├── format.test.ts      # 形式判定(拡張子優先/MIME補完/非対応null)
    └── writer.test.ts      # sanitizeFolderName
```

## 5. 主な実装ポイント

1. **サイズ計算**(resize.ts):`scale = longEdge / max(w,h)`。指定 ≥ 現長辺なら元寸法を返す(§6 拡大禁止)。四捨五入+最低1px保証。
2. **EXIF Orientation 対応**(resize.ts):`createImageBitmap(blob, { imageOrientation: 'from-image' })` でスマホ写真の回転情報を反映。lib.dom の型定義バージョン差異に左右されないよう二重キャスト。
3. **同名ファイル連番**(filename.ts):`used` セットには**小文字化済みキー**を格納する契約。Windows ファイルシステム想定で大文字小文字も同一扱い。
4. **形式判定**(format.ts):拡張子を優先し、判定できない場合のみ MIME で補完。非対応は null。
5. **WebP フォールバック**(format.ts):起動時に `toDataURL('image/webp')` で機能検出。非対応環境では PNG へ代替保存し、画面に案内表示(toBlob の黙示フォールバックによる「壊れた .webp」発生を防止)。
6. **出力二方式**(writer.ts):
   - Chrome/Edge: File System Access API(`showDirectoryPicker`)で選択フォルダ内に `resized/` を作成し直接書き込み(仕様§9どおり)
   - Firefox/Safari: `resized.zip` をダウンロード(fflate、level 0)
7. **エラー処理**(main.ts):非対応形式・デコード失敗・書き込み失敗は1枚ごとに記録し処理継続(§12)。NotAllowedError は日本語メッセージに変換。
8. **UI 応答性**:1枚処理ごとに rAF+setTimeout で制御を返し、プログレス表示中もフリーズしない。
9. **セキュリティ**:ファイル名はすべて textContent 経由でのみ描画(innerHTML 不使用)。

## 6. 発生した問題と対応

| # | 問題 | 原因 | 対応 |
|---|---|---|---|
| 1 | main.ts 作成時にエディタの入力サイズ上限(6000字推奨)超過 | 1ファイル約8000字 | 前半作成+マーカー(`// __PART2__`)置換の2段階で作成 |
| 2 | filename.test.ts の期待値誤り(10件目連番テストで a_010 を期待するも実際は a_011) | テスト側で事前投入する名前が1つ多かった | ループ範囲と期待値を修正 |
| 3 | テスト1件失敗:「大文字小文字の違いも同一扱い」で photo.jpg がそのまま返る | テストが `used` セットに生の名前(`photo.JPG`)を直接投入。実装の契約(セットは小文字化済みキー/関数経由で更新)に反していた | テストを実API経由の2呼び出しシナリオに書き換え(`photo.JPG` → `photo.jpg` が `photo_001.jpg` になることを確認)。実装は変更なし |

**教訓**:セット型を状態として共有する関数は、「誰がどう正規化して格納するか」の契約をテストにも反映させる。

## 7. テスト結果

```
Test Files  4 passed (4)
Tests       33 passed (33)
Duration    約1.6s
```

カバレッジ観点:

- calculateDimensions: 横長/縦長/正方形/拡大禁止/同寸法/実写ケース(4032×3024@1080)/四捨五入(1366×768@800)/最小1px/不正入力(RangeError 9ケース)
- resolveFileName: パススルー/image_001・002連番/拡張子違いは別名/大小文字同一扱い/桁揃え連番(a_010, a_011)/セット記録
- detectFormat: 4拡張子×大小文字/MIME補完/非対応(gif,bmp,txt,拡張子なし)/ピリオドのみ等の異常系
- sanitizeFolderName: 通常名/空白トリム/Windows予約文字置換/ドットのみ/空文字フォールバック

## 8. ビルド結果

```
vite v7.3.6 building client environment for production...
11 modules transformed.
[plugin vite:singlefile] Inlining: index-*.js / style-*.css
dist/index.html  24.21 kB / gzip: 10.68 kB
built in 535ms   (tsc --noEmit 型チェック通過)
```

JS/CSS 全部内蔵の単一HTMLのため、dist/index.html をコピーしてブラウザで開くだけでオフライン利用できる。

## 9. 既知の制限・今後の課題

- ブラウザから OS フォルダを直接開けないため、仕様§13「出力フォルダを開く」は保存先ラベル表示で代替
- 実画像を使った手動確認(D&D→変換→保存、Chrome/Edge/Firefox)は未実施 → ユーザー側で `npm run dev` 確認を推奨
- file:// で直接開いた場合の File System Access API 可否は環境依存(localhost/https 推奨)
- README「今後の候補」(JPEG品質指定・出力形式変更・最大ファイルサイズ指定・フォルダD&D)は未着手

## 10. コマンド早見表

```text
npm install      # 初回セットアップ
npm run dev      # 開発サーバー(http://localhost:5173)
npm test         # ユニットテスト(Vitest)
npm run build    # 型チェック + dist/index.html 生成
npm run preview  # ビルド成果物のプレビュー
```
